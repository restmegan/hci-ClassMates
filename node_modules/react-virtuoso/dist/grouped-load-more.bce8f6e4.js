// modules are defined as an array
// [ module function, map of requires ]
//
// map of requires is short require name -> numeric require
//
// anything defined in a previous bundle is accessed via the
// orig method which is the require for previous bundles
parcelRequire = (function (modules, cache, entry, globalName) {
  // Save the require from previous bundle to this closure if any
  var previousRequire = typeof parcelRequire === 'function' && parcelRequire;
  var nodeRequire = typeof require === 'function' && require;

  function newRequire(name, jumped) {
    if (!cache[name]) {
      if (!modules[name]) {
        // if we cannot find the module within our internal map or
        // cache jump to the current global require ie. the last bundle
        // that was added to the page.
        var currentRequire = typeof parcelRequire === 'function' && parcelRequire;
        if (!jumped && currentRequire) {
          return currentRequire(name, true);
        }

        // If there are other bundles on this page the require from the
        // previous one is saved to 'previousRequire'. Repeat this as
        // many times as there are bundles until the module is found or
        // we exhaust the require chain.
        if (previousRequire) {
          return previousRequire(name, true);
        }

        // Try the node require function if it exists.
        if (nodeRequire && typeof name === 'string') {
          return nodeRequire(name);
        }

        var err = new Error('Cannot find module \'' + name + '\'');
        err.code = 'MODULE_NOT_FOUND';
        throw err;
      }

      localRequire.resolve = resolve;
      localRequire.cache = {};

      var module = cache[name] = new newRequire.Module(name);

      modules[name][0].call(module.exports, localRequire, module, module.exports, this);
    }

    return cache[name].exports;

    function localRequire(x){
      return newRequire(localRequire.resolve(x));
    }

    function resolve(x){
      return modules[name][1][x] || x;
    }
  }

  function Module(moduleName) {
    this.id = moduleName;
    this.bundle = newRequire;
    this.exports = {};
  }

  newRequire.isParcelRequire = true;
  newRequire.Module = Module;
  newRequire.modules = modules;
  newRequire.cache = cache;
  newRequire.parent = previousRequire;
  newRequire.register = function (id, exports) {
    modules[id] = [function (require, module) {
      module.exports = exports;
    }, {}];
  };

  var error;
  for (var i = 0; i < entry.length; i++) {
    try {
      newRequire(entry[i]);
    } catch (e) {
      // Save first error but execute all entries
      if (!error) {
        error = e;
      }
    }
  }

  if (entry.length) {
    // Expose entry point to Node, AMD or browser globals
    // Based on https://github.com/ForbesLindesay/umd/blob/master/template.js
    var mainExports = newRequire(entry[entry.length - 1]);

    // CommonJS
    if (typeof exports === "object" && typeof module !== "undefined") {
      module.exports = mainExports;

    // RequireJS
    } else if (typeof define === "function" && define.amd) {
     define(function () {
       return mainExports;
     });

    // <script>
    } else if (globalName) {
      this[globalName] = mainExports;
    }
  }

  // Override the current require with this new one
  parcelRequire = newRequire;

  if (error) {
    // throw error from earlier, _after updating parcelRequire_
    throw error;
  }

  return newRequire;
})({"grouped-load-more.tsx":[function(require,module,exports) {
"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = App;

var React = _interopRequireWildcard(require("react"));

var _src = require("../src");

var _faker = _interopRequireDefault(require("faker"));

var _lodash = require("lodash");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function () { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } if (obj === null || typeof obj !== "object" && typeof obj !== "function") { return { default: obj }; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

function _objectWithoutProperties(source, excluded) { if (source == null) return {}; var target = _objectWithoutPropertiesLoose(source, excluded); var key, i; if (Object.getOwnPropertySymbols) { var sourceSymbolKeys = Object.getOwnPropertySymbols(source); for (i = 0; i < sourceSymbolKeys.length; i++) { key = sourceSymbolKeys[i]; if (excluded.indexOf(key) >= 0) continue; if (!Object.prototype.propertyIsEnumerable.call(source, key)) continue; target[key] = source[key]; } } return target; }

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

function _iterableToArrayLimit(arr, i) { if (typeof Symbol === "undefined" || !(Symbol.iterator in Object(arr))) return; var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

var getUser = function getUser() {
  var firstName = _faker.default.name.firstName();

  var lastName = _faker.default.name.lastName();

  return {
    name: "".concat(firstName, " ").concat(lastName),
    initials: "".concat(firstName.substr(0, 1)).concat(lastName.substr(0, 1)),
    description: _faker.default.company.catchPhrase(),
    avatar: _faker.default.internet.avatar()
  };
};

var sortUser = function sortUser(a, b) {
  if (a.name < b.name) {
    return -1;
  }

  if (a.name > b.name) {
    return 1;
  }

  return 0;
};

var useGroupedUsers = function useGroupedUsers(count) {
  var allUsers = (0, React.useMemo)(function () {
    return new Array(count).fill(true).map(getUser).sort(sortUser);
  }, [count]);
  var loadedCount = (0, React.useRef)(0);
  var loadedUsers = (0, React.useRef)([]);
  var groups = (0, React.useRef)([]);

  var _useState = (0, React.useState)(false),
      _useState2 = _slicedToArray(_useState, 2),
      endReached = _useState2[0],
      setEndReached = _useState2[1];

  var _useState3 = (0, React.useState)([]),
      _useState4 = _slicedToArray(_useState3, 2),
      groupCounts = _useState4[0],
      setGroupCounts = _useState4[1];

  var loadMore = function loadMore() {
    if (!endReached) {
      setTimeout(function () {
        loadedCount.current += 50; // in a real world scenario, you would fetch the next
        // slice and append it to the existing records

        loadedUsers.current = allUsers.slice(0, loadedCount.current); // the code below calculates the group counts
        // for the users loaded so far;
        // this should be performed on the server too

        var groupedUsers = (0, _lodash.groupBy)(loadedUsers.current, function (user) {
          return user.name[0];
        });
        groups.current = Object.keys(groupedUsers);
        setGroupCounts(Object.values(groupedUsers).map(function (users) {
          return users.length;
        }));

        if (loadedCount.current === 500) {
          setEndReached(true);
        }
      }, 1500);
    }
  };

  return {
    loadMore: loadMore,
    endReached: endReached,
    groupCounts: groupCounts,
    users: loadedUsers.current,
    groups: groups.current
  };
};

var components = {
  Footer: function Footer() {
    return React.createElement("div", null, "Footer");
  },
  List: React.forwardRef(function (_ref, listRef) {
    var style = _ref.style,
        children = _ref.children;
    return React.createElement("div", {
      ref: listRef,
      style: style
    }, children);
  }),
  Item: function Item(_ref2) {
    var children = _ref2.children,
        props = _objectWithoutProperties(_ref2, ["children"]);

    return React.createElement("div", Object.assign({}, props, {
      style: {
        margin: 0
      }
    }), children);
  },
  Group: function Group(_ref3) {
    var children = _ref3.children,
        props = _objectWithoutProperties(_ref3, ["children"]);

    return React.createElement("div", Object.assign({}, props), children);
  }
};
var Style = {
  height: '350px',
  width: '400px'
};

function App() {
  var _useGroupedUsers = useGroupedUsers(500),
      loadMore = _useGroupedUsers.loadMore,
      groupCounts = _useGroupedUsers.groupCounts,
      users = _useGroupedUsers.users,
      groups = _useGroupedUsers.groups;

  (0, React.useEffect)(loadMore, [loadMore]);
  return React.createElement(_src.GroupedVirtuoso, {
    components: components,
    style: Style,
    groupCounts: groupCounts,
    groupContent: function groupContent(index) {
      return React.createElement("div", null, "Group ", groups[index]);
    },
    overscan: 400,
    endReached: function endReached(value) {
      console.log(value);
      loadMore();
    },
    itemContent: function itemContent(index) {
      return React.createElement("div", null, React.createElement("div", null, users[index].initials), React.createElement("div", null, React.createElement("strong", null, users[index].name)), React.createElement("div", null, users[index].description));
    }
  });
}
},{"react":"../node_modules/react/index.js","../src":"../src/index.tsx","faker":"../node_modules/faker/index.js","lodash":"../node_modules/lodash/lodash.js"}]},{},[], null)